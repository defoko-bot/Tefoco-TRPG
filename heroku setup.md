variaveis: BOT_TOKEN (bot do token bruh)
           STATUS (richpresence do bot)
           SERVER_NAME (nome do server)
          
------------------------------------

vars: BOT_TOKEN (bot token bruh)
      STATUS (rlly?)
      SERVER_NAME (bruh)
